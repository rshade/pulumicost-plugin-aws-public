# API Contracts (finfocus.v1)

This feature uses the standard `CostSourceService` from `finfocus.v1`.

## Methods Used

### GetProjectedCost
- **Request**: `GetProjectedCostRequest` containing `ResourceDescriptor`.
- **Response**: `GetProjectedCostResponse` containing cost and billing details.

## Relevant Enums
- `ErrorCode`: Used for reporting missing data or invalid resources.
